import {
    Item, Input, Label
} from 'native-base';
import { TouchableOpacity } from 'react-native';

export interface IStylesBoardScreen {
    item: Item;
    input: Input;
    label: Label;
    touchableDatePicker: TouchableOpacity;

}